# DeepResearchDemo -- DataBase

## PostgresSQL
1. 安装:
`sudo apt update`
`sudo apt install postgresql postgresql-contrib -y`
2. 启动
安装好后,会自动注册为systemd服务,也可通过如下命令启动
`sudo pg_ctlcluster 16 main start`
3. 创建用户
4. 创建数据表 & 给相关用户赋权

## Milvus 向量数据库
